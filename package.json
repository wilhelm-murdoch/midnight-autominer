{
  "name": "midnight-sm-autominer",
  "version": "1.0.0",
  "main": "dist/autominer.js",
  "types": "dist/autominer.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "serve": "npx http-server ./dist -p 3000 --cors",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
